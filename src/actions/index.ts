import areasActions from './areasActions';
import selectedAreaActions from './selectedAreaActions';
import productsActions from './productsActions';

const allActions = {
  areasActions,
  selectedAreaActions,
  productsActions,
};

export default allActions;
